<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ditt Träningsprogram</title>
    <link rel="stylesheet" href="/training_plan.css">
</head>
<body>
    <div class="container">
        <div class="training-plan">
            <h1>Ditt Träningsprogram</h1>
            <p>Här är ditt personliga träningsprogram baserat på dina val:</p>

            <!-- Träningsprogram -->
            <div>
                <h2>Träningsschema:</h2>
                <% training_plan.each do |day| %>
                    <div>
                        <strong><%= day[:day] %>:</strong> <%= day[:type] %>
                        <% if day[:type] == "Träningspass" %>
                            <ul>
                                <% day[:exercises].each do |exercise| %>
                                    <li><%= exercise %> - Tid: <%= day[:time] %> min</li>
                                <% end %>
                            </ul>
                        <% end %>
                    </div>
                <% end %>
            </div>

            <br>
            <a href="/">Tillbaka</a>
        </div>

        <div class="calendar">
            <% week_schedule.each_with_index do |day, index| %>
                <div class="day <%= 'training' if day == 'Träningspass' %> <%= 'rest' if day == 'Vilodag' %>">
                    <%= get_day_name(index) %> <br> <%= day %>
                </div>
            <% end %>
        </div>
    </div>
</body>
</html>

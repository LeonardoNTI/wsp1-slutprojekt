<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Träningsplan</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <!-- Inloggningsinfo och utloggning -->
    <div class="user-info-box" style="position: absolute; top: 10px; right: 20px;">
        <% if current_user %>
            <span class="user-info-text">Inloggad som: <%= current_user['username'] %></span>
            <form action="/logout" method="get" style="display: inline;">
                <button type="submit" class="logout-btn">Logga ut</button>
            </form>
        <% else %>
            <span class="user-info-text">Ej inloggad</span>
        <% end %>
    </div>

    <!-- Adminflik -->
    <% if session[:role] == 'admin' %>
        <nav class="admin-nav-box" style="position: absolute; top: 10px; left: 20px;">
            <a href="/admin" class="admin-nav-link">🛠 Adminpanel</a>
        </nav>
    <% end %>

    <!-- Sidinnehåll -->
    <%= yield %>
</body>
</html>
